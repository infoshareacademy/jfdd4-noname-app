import { REQUEST_STOPS, RECEIVE_STOPS } from '../bus-stops/actionTypes'
import { REQUEST_BUSES, RECEIVE_BUSES } from '../bus-lines/actionTypes'

const initialState = {
    stops: [],
    buses: []
}

